package lesson07.hometask;
import org.openqa.selenium.WebDriver;

public class SearchPage extends BasePage implements SearchPageLocators {

        private WebDriver driver;

        SearchPage(WebDriver driver) {
            super(driver);
        }


    }

